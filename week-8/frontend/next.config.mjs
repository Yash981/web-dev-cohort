/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['appxcontent.kaxa.in','res.cloudinary.com','images.unsplash.com'], // Add the domain here
    },
};

export default nextConfig;
